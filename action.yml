name: 'AWS Go CDK'
description: 'Run CDK commands.'
inputs:
  command:
    description: 'Command'
    default: 'cdk deploy'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.command }}
